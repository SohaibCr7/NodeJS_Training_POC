{
    "extends": "./tsconfig.json",
    "compilerOptions": {
      "noEmit": true,
      // be explicit about types included
      // to avoid clashing with Jest types
      "types": ["jest"]
    },
    "include": ["**/*.spec.ts", "**/*.test.tsx", "**/*.test.ts"],
    "exclude": ["cypress/**/*"]
  }
  